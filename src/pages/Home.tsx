import { ReactElement } from 'react';
import Header from '../components/Header';

const Home = (): ReactElement => {
  return (
    <div>
      <Header />
      Home
    </div>
  );
};

export default Home;
